# Auto-generated by Matugen — Material You colors for tmux

# --- Color Variables ---
set -gq @thm_bg                           "{{colors.surface.default.hex}}"
set -gq @thm_fg                           "{{colors.on_surface.default.hex}}"
set -gq @thm_primary                      "{{colors.primary.default.hex}}"
set -gq @thm_on_primary                   "{{colors.on_primary.default.hex}}"
set -gq @thm_secondary                    "{{colors.secondary.default.hex}}"
set -gq @thm_on_secondary                 "{{colors.on_secondary.default.hex}}"
set -gq @thm_tertiary                     "{{colors.tertiary.default.hex}}"
set -gq @thm_surface_low                  "{{colors.surface_container_low.default.hex}}"
set -gq @thm_surface                      "{{colors.surface_container.default.hex}}"
set -gq @thm_surface_high                 "{{colors.surface_container_high.default.hex}}"
set -gq @thm_outline                      "{{colors.outline_variant.default.hex}}"
set -gq @thm_text_variant                 "{{colors.on_surface_variant.default.hex}}"
set -gq @thm_error                        "{{colors.error.default.hex}}"

# --- Status Bar ---
set -g status-style                       "bg=#{@thm_bg},fg=#{@thm_fg}"
set -g status-left-length 40
set -g status-right-length 60
set -g status-left                        "#[bg=#{@thm_primary},fg=#{@thm_on_primary},bold] #S #[bg=#{@thm_bg},fg=#{@thm_primary}]"
set -g status-right                       "#[fg=#{@thm_text_variant}] %H:%M #[bg=#{@thm_surface_high},fg=#{@thm_fg}] #h "

# --- Window Tabs ---
set -g window-status-format               "#[fg=#{@thm_text_variant}] #I:#W "
set -g window-status-current-format       "#[bg=#{@thm_surface_high},fg=#{@thm_primary},bold] #I:#W "
set -g window-status-separator            ""

# --- Pane Borders ---
set -g pane-border-style                  "fg=#{@thm_outline}"
set -g pane-active-border-style           "fg=#{@thm_outline}"

# --- Message & Command Bar ---
set -g message-style                      "bg=#{@thm_surface_high},fg=#{@thm_fg}"
set -g message-command-style              "bg=#{@thm_surface_high},fg=#{@thm_fg}"

# --- Copy Mode ---
set -g mode-style                         "bg=#{@thm_primary},fg=#{@thm_on_primary}"

# --- Window Activity ---
set -g window-status-activity-style       "fg=#{@thm_tertiary}"
set -g window-status-bell-style           "fg=#{@thm_error}"
