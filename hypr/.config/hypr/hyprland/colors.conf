# exec = export SLURP_ARGS='-d -c DBE4E6BB -b 414B4C44 -s 00000000'

general {
    col.active_border = rgba(E3E2E239)
    col.inactive_border = rgba(91919130)
}

misc {
    background_color = rgba(121414FF)
}

plugin {
    hyprbars {
        # Honestly idk if it works like css, but well, why not
        bar_text_font = Rubik, Geist, AR One Sans, Reddit Sans, Inter, Roboto, Ubuntu, Noto Sans, sans-serif
        bar_height = 30
        bar_padding = 10
        bar_button_padding = 5
        bar_precedence_over_border = true
        bar_part_of_window = true

        bar_color = rgba(121414FF)
        col.text = rgba(E3E2E2FF)


        # example buttons (R -> L)
        # hyprbars-button = color, size, on-click
        hyprbars-button = rgb(E3E2E2), 13, 󰖭, hyprctl dispatch killactive
        hyprbars-button = rgb(E3E2E2), 13, 󰖯, hyprctl dispatch fullscreen 1
        hyprbars-button = rgb(E3E2E2), 13, 󰖰, hyprctl dispatch movetoworkspacesilent special
    }
}

windowrulev2 = bordercolor rgba(B8CACDAA) rgba(B8CACD77),pinned:1